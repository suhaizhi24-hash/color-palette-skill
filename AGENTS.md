# Repository Instructions

## Photography Knowledge Consumer

- 本仓库是中央摄影知识树的 Consumer，不是摄影、色彩或调色知识的最高来源。
- 凡任务涉及色彩分析、调色、LUT、Tone、Hue、Saturation、Skin Tone、Camera Emulation、Film Emulation、Grain、Halation、Bloom 或 Glow，先从仓库根目录读取 `../../摄影知识树/KNOWLEDGE_REGISTRY.md`，再按任务读取对应 Memory 与治理文件。
- 中央入口和读取边界以 `docs/PHOTOGRAPHY_KNOWLEDGE_CONTRACT.md` 为准；不要把中央 Memory 复制进本仓库。
- 如果中央知识不可访问，明确报告 `knowledge source unavailable`，不得声称已经读取或应用中央规则。
- 中央知识树负责知识定义、Evidence、Knowledge Status、Rule ID 与科学边界；本 Skill 负责图片输入处理、字段组织、报告模板、卡片布局和输出格式。
- Rule ID 默认只用于内部推理、debug、research、实验、QA 与知识回流；普通用户报告不批量展示 Rule ID。
- 不得把 `CREATIVE_HEURISTIC` 写成科学证明，不得把 `PROVISIONAL` 写成确定事实，不得把 `PROJECT_STANDARD` 写成所有摄影工作流的普遍定律。只有 `VALIDATED` 可声明已完成项目实证验证。
- 保留已验收行为：光源、光质、光比分别判断；颗粒继续分粗/细；正式报告默认不显示置信度；LUT 与空间素材特效保持分离。
