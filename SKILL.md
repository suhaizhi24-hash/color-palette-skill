# 调色盘 / 色彩卡片 SKILL V0.12.0（中文官方框架版）

## 定位
Local-first / Zero-token 的专业照片色彩分析工具。

## 最高规则 0：官方语言必须为中文
- 所有用户可见的模块标题、结论、判断标签、降级提示与报告说明，必须使用中文（zh-CN）作为官方语言。
- RGB、HEX、L*、a*、b*、HSV、P5/P50/P95 等标准色彩/统计符号可保留，但解释必须为中文。
- 程序内部可以使用稳定英文键名；对外必须提供中文官方标题与中文正式解释。
- 禁止中英模块名称混用，禁止把内部状态码直接展示给用户。

## 最高规则 1：原图保真
只分析，不修改照片任何色彩参数。

允许：EXIF方向修正、ICC读取/标准化、缩放与排版裁切。
禁止：曝光、白平衡、色相、饱和度、对比度调整；LUT、滤镜、美颜、锐化、降噪、柔焦、颗粒添加；生成式重绘。

## 最高规则 2：Zero-token
当前核心分析、规则、文案与报告渲染全部本地完成，不调用 OpenAI API，不要求 API Key。

## 最高规则 3：输出完整性
照片分析任务只有在以下文件成功生成后才算完成：
- `{filename}_analysis.json`
- `{filename}_color_report.png`

正式视觉报告只输出 PNG，不再同步生成 JPG/JPEG。
禁止只输出文字后结束。

# 官方固定七模块
所有照片必须按照下列顺序输出；不得因照片类型而删除、重命名或改变顺序：

1. **影调结构**
2. **明暗关系**
3. **色彩浓度**
4. **白平衡&色相**
5. **影调色卡**
6. **肤色锚点**
7. **素材特效&光线构成**

模块信息不足时只允许“模块内降级”，不允许删除模块。

## 1. 影调结构
负责：高调 / 中间调 / 低调、暗部/中间调/高光分布、黑白端像素状态与影调重心。

正式报告只展示：
- 影调结构结论；
- 黑白两端是否碰壁及其画面表现。

禁止在用户可见报告中使用“依据”“数据依据”等内部分析标题。

## 2. 明暗关系
负责：明暗跨度、黑位密度、高光余量、黑白端像素碰壁与中间调过渡。

正式等级表达固定为：
- 明暗关系小，对比度低；
- 明暗关系中等，对比度中；
- 明暗关系大，对比度高。

禁止在用户可见报告中使用“依据”“数据依据”等内部分析标题。
局部高光剪切不得自动等同“整幅过曝”。

## 3. 色彩浓度
负责：低/中/高饱和占比、综合色彩状态、色彩是否克制与溢色风险。

正式结论必须使用：
- 色彩饱和度低；
- 色彩饱和度中；
- 色彩饱和度高。

不得在正式报告中使用“浓度高/中/低”作为等级结论。

## 4. 白平衡&色相
必须在同一模块内输出两项正式结果：
- 白平衡结论；
- 暗部 / 中间调 / 高光的综合色相。

可信中性色 <5% 时，白平衡只输出“可信中性色不足，暂不下确定结论”。
环境主色不得直接等同于白平衡错误。
用户可见报告不得展示内部产品说明、判断流程说明或算法降级原理。

## 5. 影调色卡
固定五级：深黑 / 阴影 / 中间调 / 亮部 / 高光。
来源：各 L* 区间内真实像素主导色簇。
显示：RGB / HEX / L* / 区间占比。

## 6. 肤色锚点
- 单人：① 苹果肌主锚点；② 额头副锚点。
- 主锚点优先中间调稳定肤色，不以最亮区域为原则。
- 额头副锚点只用于高光/环境光变化参考，阈值更严格。
- 多人：不得合并为一个肤色锚点。
- 无人像或低置信度：模块保留，中文说明“不输出肤色数值”。
- 宁可隐藏，也不得编造。

### 肤色锚点非侵入式展示规则（最高显示规则）
正式 PNG 报告中的原图区域必须保持干净，不得叠加：
- ① / ② 数字圆点；
- 取样圆圈、十字线、方框；
- 人脸框、关键点；
- 锚点名称或色块。

苹果肌主锚点与额头副锚点仍需在后台完成分析，并在“肤色锚点”模块中展示 RGB / HEX / LAB 与有效状态。

调试模式可单独输出带标记的 `*_skin_debug.png`，但：
- 默认关闭；
- 不属于正式报告；
- 不得替代正式 PNG；
- 不得与公开分享报告混用。

### 肤色取样截图展示规则
正式报告的“肤色锚点”模块必须在锚点有效时展示两张取样区域截图：

1. 苹果肌主锚点取样截图；
2. 额头副锚点取样截图。

显示规范：
- 默认比例：1:1；
- 可选比例：4:3；
- 同一份报告内两张截图必须使用相同的比例和尺寸；
- 截图必须直接裁取自原始照片像素；
- 只允许等比裁切、缩放和圆角遮罩；
- 禁止修改曝光、白平衡、色相、饱和度、对比度、锐度或肤色；
- 截图内部不得显示十字线、圆点、数字、取样框或其他标记；
- 原图区域继续保持干净，不显示任何肤色锚点标记。

锚点无效时：
- 保留对应位置；
- 使用“样本不足”占位卡；
- 不得伪造肤色截图或数值。

## 7. 素材特效&光线构成
模块内固定两部分：

### 素材特效
只展示高置信度“已识别”和“未发现明显”。证据不足时中文说明“不做确定判断”。

### 光线构成
只保留：
- 光源
- 光质
- 光比

P1-C 校准完成前，证据不足时必须中文降级，不允许强判。

# 固定流程
图片 → 原图校验 → sRGB工作空间 → 像素分析 → 确定性规则 → 中文本地文案模板 → 4:3报告 → PNG/JSON。

# 报告
- 4:3
- 1600×1200 px
- 横 / 竖 / 方构图自动适配
- 原图预览不得发生创造性色彩调整
- 正式报告的分析主体仅使用官方固定七模块；研发状态、自动化状态、内部规则状态不得混入正式色彩分析框架。

# 文案
“调色师”只负责中文专业表达；结论必须由“数据 → 规则 → 中文模板”产生。

用户可见正式报告：
- 影调结构：只展示正式结论；
- 明暗关系：使用“明暗关系小/中等/大，对比度低/中/高”；
- 色彩浓度：使用“色彩饱和度高 / 中 / 低”；
- 白平衡&色相：只展示正式结果；
- 不展示“依据”“数据依据”“分析说明”等内部栏目名称。

详细数值与判断过程继续保存在 analysis.json 中。



### 字体规范（正式报告）
正式报告的中文字体规范调整为：

- 首选字体：PingFang SC（苹方简）；
- 回退字体：Noto Sans CJK SC / Source Han Sans SC；
- 数字与英文可跟随同套无衬线字体；
- 若运行环境未安装 PingFang SC，可自动回退，不得阻塞正式报告输出。

当前结论：
- 设计规范层面：同意使用苹方简作为正式报告首选字体；
- 工程实现层面：必须保留跨平台回退字体机制。

### 影调色卡排版规范补充
- 影调色卡模块允许增大色卡间距；
- 优先保证“名称 / RGB / HEX / L* / 区间占比”的可读性；
- 不得因横向拥挤导致信息堆叠或压缩过度；
- 建议适度降低单卡宽度占比，增大卡间留白。

### 肤色锚点文字显示规范补充
- “肤色锚点”标题右侧的灰色辅助说明文字可移除；
- 肤色模块内部仅保留必要字段；
- 非必要的灰色解释性文案应删除，避免分散注意力。


### 影调色卡与底部模块联合排版规范
为避免影调色卡出现横向和纵向拥挤，正式 1600×1200 报告采用以下约束：

- 影调色卡模块建议高度不低于 210 px；
- 五张色卡之间的净间距建议为 22–26 px；
- 每张色卡使用独立的轻描边信息卡，避免五组数据视觉粘连；
- 色块、RGB、HEX/L*、区间占比之间保持独立行距；
- 影调色卡与下方“肤色锚点 / 素材特效&光线构成”之间保留不少于 16 px 的模块间距；
- 为释放影调色卡空间，肤色截图默认可缩至 136–144 px，但不得牺牲肤色识别性；
- 下方两个模块允许减少无效留白，但不得压缩标题、截图或关键数据；
- 横图、竖图、方图必须保持同一信息层级与视觉节奏。

### 当前版式冻结结论
- 字体：正式方向确认 PingFang SC（苹方简）优先，跨平台回退保持不变；
- 影调色卡：采用独立五卡结构和更大留白；
- 肤色锚点：保留截图与必要数据，移除灰色辅助说明；
- 主 UI 框架暂不继续改动，后续以批量渲染稳定性为主。


# 正式输出格式协议（PNG Only）

## 官方视觉格式
正式色彩分析报告统一输出：

- PNG（唯一官方图片格式）

不再默认生成：

- JPG / JPEG

## 保留的机器数据
`analysis.json` 继续保留，用于：

- 保存原始分析数值；
- 自动回归测试；
- 批量处理；
- 后续重新渲染报告。

JSON 不属于第二种视觉报告格式，因此不违反“只输出一种图片格式”的要求。

## 采用 PNG 的原因

- 无损保存文字、线条、色块与肤色截图；
- 不产生 JPG 压缩块、边缘模糊和色卡污染；
- 渲染参数更少，自动化流程更简单；
- 更适合作为开源项目的标准验收源文件；
- 便于对报告进行像素级回归测试。

## 输出目录

```text
output/
├── {filename}_analysis.json
└── {filename}_color_report.png
```

## JPG 处理
JPG 不属于当前核心输出。未来如确有社交媒体分享需求，可由用户在报告生成后自行转换，但不得影响核心分析与 PNG 正式报告。



# Round 9 工程执行补充

- 正式 CLI：`color-palette <图片路径> --output <目录>`；
- 正式输出仅包含 `{filename}_analysis.json` 与 `{filename}_color_report.png`；
- CLI、Pipeline、Renderer 与自动测试必须共享同一输出协议；
- 透明 PNG 的完全透明像素不得参与色彩统计；
- WebP 必须作为正式支持格式；
- 正式 PNG 报告不得显示人脸框、关键点或肤色取样标记；
- 肤色截图仅出现在“肤色锚点”模块；
- 素材特效与光线构成在证据不足时必须中文降级，不得强判。


# P1 规则校准 V0.10.0

## 影调结构
- 高调：L* 中位数 ≥ 62；或高光占比 ≥ 42% 且暗部占比 ≤ 32%。
- 低调：L* 中位数 ≤ 35；或暗部占比 ≥ 55% 且高光占比 ≤ 25%。
- 其他情况：中间调结构。
- 目的：避免天空、白墙等单一大面积亮区把整幅画面误判为高调。

## 明暗关系
- P95−P5 ≥ 70 L*：明暗关系大，对比度高。
- 50–70 L*：明暗关系中等，对比度中。
- <50 L*：明暗关系小，对比度低。

## 色彩浓度
- HSV S 中位数 <0.20：色彩饱和度低。
- 0.20–0.45：色彩饱和度中。
- ≥0.45：色彩饱和度高。

## 白平衡
白平衡必须同时满足：
- 可信近中性色占比 ≥5%；
- 近中性色在 4×4 网格中的空间覆盖 ≥25%。

任一条件不满足时，只输出“可信中性色不足，暂不下确定结论”。

## 多人物
- 当检测到两张或以上人脸时，肤色状态固定为“多人不合并”；
- 正式报告不显示单一苹果肌或额头锚点；
- 不得把多人物肤色平均为一个值。
# Golden Dataset 回归协议

## 目的
人工确认的分析结果必须写入外部 Ground Truth 文件，并通过 SHA-256 与本地原图匹配。

## 隐私规则
- 开源仓库不得包含用户提供的私人照片；
- 私人 Ground Truth 可保存图片 SHA-256 与确认标签，但必须与公开源码包分离；
- 公共仓库只提供 Schema、合成示例与验证工具；
- 验证过程完全本地运行，不上传图片，不调用 OpenAI API。

## 回归字段
必须字段：
- 影调结构；
- 明暗关系；
- 色彩饱和度；
- 白平衡正式结论。

建议字段：
- 肤色状态；
- 人脸数量。

建议字段用于暴露跨平台人脸检测差异；默认不阻塞核心色彩规则通过，但必须记录。

## 命令
```bash
color-palette-golden ./images \
  --ground-truth ./ground_truth.json \
  --output ./golden_validation_report.json \
  --strict-missing
```

# V0.12.0 开源发布加固

## 跨平台字体
- macOS 首选 PingFang SC（苹方简）；
- 跨平台回退 Noto Sans CJK SC / Source Han Sans SC；
- Windows 在上述字体均缺失时可使用 Microsoft YaHei / SimHei 作为系统兜底；
- 允许通过 `COLOR_PALETTE_FONT_REGULAR` 与 `COLOR_PALETTE_FONT_BOLD` 指定本地字体；
- 仓库不得打包或分发字体文件；
- 字体完全缺失时允许内置紧急回退，不得阻塞正式报告输出。
- `color-palette-doctor` 必须明确报告中文字体是否可用及是否进入紧急回退。

## 格式与色彩管理
- EXIF Orientation 必须先修正再分析；
- 嵌入 ICC 可读取时必须转换至 sRGB；
- ICC 失败时必须在 JSON 中记录并安全降级，不得伪装为成功；
- 完全透明像素不得参与分析；
- 棋盘格只用于透明图预览，不得进入统计；
- JPG、PNG、WebP 必须通过公开合成样本回归。

## 人脸后端
- 跨平台默认使用 OpenCV；
- V0.12.0 支持并测试 OpenCV 4.9 至 4.x（`>=4.9,<5`），暂不承诺 OpenCV 5.x；
- dlib 为可选增强，不得成为核心依赖；
- dlib 缺失或失败时安全降级至 OpenCV；
- `none` 允许关闭肤色分析，但不得影响核心色彩分析；
- 分析 JSON 必须记录请求后端、实际后端、可用后端与降级状态。

## 环境诊断
发布版本必须提供 `color-palette-doctor`，用于检查：
- JPG / PNG / WebP；
- LittleCMS / ICC；
- 中文字体；
- OpenCV / dlib；
- Python 与依赖版本；
- Zero-token 与 PNG-only 输出契约。

## 公开示例与隐私
- 公开仓库只允许程序生成且经人工审核的合成示例图；
- 人工审核来源表与生成清单必须独立，生成器不得写入来源表；
- 示例必须在两份登记中保持相同的 SHA-256 与许可；
- 公开示例不得含 GPS EXIF；
- 私人照片与私人 Ground Truth 必须与公开源码物理分离；
- 必须先扫描 checkout 原貌；如更新夹具，生成后再次扫描，之后才能打包与发布。

## 光线与素材特效
- P1-C 在正式算法门禁前，先使用独立公开 Golden Dataset；
- 当前只验证人工标签 Schema、文件与哈希；
- 未经人工验收的光线/特效算法不得成为正式结论。

## 跨平台通过规则
Windows、macOS、Ubuntu 必须由 GitHub Actions 或实机实际执行后才能标记为通过。本地 Linux 结果不得替代其他操作系统结果。
