# 安全政策

## 支持范围

当前公开 Beta 仅维护 `0.12.x`。请先确认问题能够在当前 PR 或最新已发布版本中复现；旧版本的问题可能需要先升级后再判断。

## 私下报告安全问题

请优先使用 GitHub 仓库的私密漏洞报告入口：

1. 打开仓库的 **Security → Advisories**；
2. 选择 **Report a vulnerability**；
3. 仅提交复现所需的最少信息，并说明受影响版本、平台、影响范围和建议的修复或缓解方式。

如果仓库暂未显示私密报告入口，请只创建一条不含漏洞细节、照片、凭据或个人数据的公开 Issue，请求维护者提供私密沟通渠道。维护者确认私密渠道前，不要公开披露可利用细节。

## 严禁公开上传的内容

请勿在公开 Issue、Discussion、Pull Request、评论或附件中上传：

- 用户照片、私人样片、真实人像 Ground Truth 或验收图；
- API Key、Personal Access Token、密码、私钥、Cookie 或其他凭据；
- EXIF GPS、拍摄地点、设备序列号、姓名、联系方式或其他个人数据；
- 未脱敏的 `analysis.json`、错误日志、命令历史或绝对路径；
- 字体文件或来源、许可不明确的素材。

若复现必须依赖图片，请先使用仓库中的公开合成夹具。确实无法使用合成数据复现时，先通过私密漏洞报告与维护者确认安全传输、保留和删除方式；未经确认不得发送原图。

## 脱敏要求

提交诊断材料前，请至少删除或替换：

- 文件名、用户名、主目录和其他本地绝对路径；
- 图片 SHA-256、ICC 配置文件名称或哈希等可关联原文件的信息；
- 人脸框、肤色取样位置及不影响复现的肤色数值；
- 环境变量、凭据、访问地址和账号标识。

只保留复现所必需的版本号、操作系统、Python/OpenCV/Pillow 版本、错误类型和已脱敏调用步骤。

## 本地运行与依赖边界

本项目核心功能采用 Local-first / Zero-token 设计：不依赖 OpenAI API、Anthropic API 或其他付费大模型接口，不要求用户提供 API Key，也不会为核心分析上传照片。运行时依赖仍应从可信来源安装，并在报告依赖漏洞时注明包名和已安装版本。

## 协调披露

请给维护者合理的确认、复现和修复时间。在维护者确认修复方案和公开时间前，请勿发布漏洞细节、利用代码或含敏感数据的截图。维护者会尽可能在私密渠道中更新处理状态。
